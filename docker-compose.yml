services:
  rome:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - rome-data:/data
    environment:
      - DATABASE_PATH=/data/rome.db
      - JWT_SECRET=${JWT_SECRET:-change-me-in-production}
    restart: unless-stopped

volumes:
  rome-data:
